# PII-protected skills (not ready for public)
alight-yoga-booking/
amc-movie-booking/
email-triage/
great-clips/
health-connect-db/
goplaces.bak/

# Engram memory data — stored outside repo at workspace/memory/graph.json
# (No ignore rule needed — it's outside the git tree entirely)

# Backup files
*.bak
*/*.bak
